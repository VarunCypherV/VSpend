apiVersion: v1
kind: Service
metadata:
  name: vspendtrack-frontend
spec:
  selector:
    app: vspendtrack-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort  # Expose outside the cluster
